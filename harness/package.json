{
  "name": "insight-retreats-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sub-agent harness that discovers California retreat centers and writes catalogue entries.",
  "scripts": {
    "discover": "tsx src/index.ts",
    "discover:region": "tsx src/index.ts --region",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
